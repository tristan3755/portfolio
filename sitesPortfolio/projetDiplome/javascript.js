window.addEventListener('load',(event)=>{

header()


})


function header(){
    let monHeader=document.querySelector('header')
    monHeader.style.height="80px"
    monHeader.style.transition="2s"
    monHeader.style.opacity="1"
}

